import React from 'react';
import { createRoot } from 'react-dom/client';

// Placeholder shell until M5 (PRD §9).
function App() {
  return <main style={{ fontFamily: 'system-ui', padding: 32 }}>Westline HR Agent — UI arrives at M5.</main>;
}

createRoot(document.getElementById('root')!).render(<App />);
